// src/store/index.ts
import { createStore } from 'vuex';

const store = createStore({
  state: {
    // Ваше состояние
  },
  mutations: {
    // Ваши мутации
  },
  actions: {
    // Ваши действия
  },
  modules: {
    // Ваши модули
  },
});

export default store;
